import React from 'react';

const LoginPage = () => {
    return (
        <div>

            <button

                className="btn btn-outline"
            >Continue with Google</button>
        </div>
    );
};

export default LoginPage;