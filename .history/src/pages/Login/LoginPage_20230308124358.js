import React from 'react';

const LoginPage = () => {
    return (
        <div>
            <button className="btn btn-primary text-center">Google</button>
        </div>
    );
};

export default LoginPage;