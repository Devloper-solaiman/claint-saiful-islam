import React from 'react';

const LoginPage = () => {
    const [sign]
    return (
        <div>
            <button className="btn">Google</button>
        </div>
    );
};

export default LoginPage;